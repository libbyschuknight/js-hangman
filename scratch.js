// reg exp to only allow one char
//  var re = /^\w{1}$/;
// var re = new RegExp('^\w{1}$');
// or check that response.guess just has one char.

// grab first letter
console.log('sub', string.substr(0, 1));
